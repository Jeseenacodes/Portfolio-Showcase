/**
 * Vercel serverless entry point.
 * Exports the Express app as the default handler — Vercel handles the HTTP
 * lifecycle, so no app.listen() call is needed here.
 */
export { default } from "./app";
